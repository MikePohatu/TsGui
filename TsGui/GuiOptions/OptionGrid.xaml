<UserControl x:Class="TsGui.OptionGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TsGui"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="300">
    <Grid x:Name="Grid" Margin="0,0,0,0" ShowGridLines="{Binding ShowGridLines}" Width="{Binding Width}" Height="{Binding Height}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="LabelColumn"  Width="{Binding LabelWidth}"></ColumnDefinition>
            <ColumnDefinition x:Name="ControlColumn" Width="{Binding ControlWidth}"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition  Height="{Binding Height}"></RowDefinition>
        </Grid.RowDefinitions>
    </Grid>
</UserControl>
